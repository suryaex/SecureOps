# SecureOps Agent — Windows
# Kompatibel dengan Windows 10/11 + Windows Server 2019/2022, Python 3.10-3.13

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
python-multipart>=0.0.9,<1.0
psutil>=5.9,<7.0
websockets>=12.0,<15.0

# Windows-specific
pywinpty>=2.0,<3.0    # ConPTY untuk terminal realtime
pywin32>=306          # opsional, untuk Windows-specific APIs (di-load lazy)
